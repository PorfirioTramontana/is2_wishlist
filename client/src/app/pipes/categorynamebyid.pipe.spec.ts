import { CategorynamebyidPipe } from './categorynamebyid.pipe';

describe('CategorynamebyidPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorynamebyidPipe();
    expect(pipe).toBeTruthy();
  });
});
